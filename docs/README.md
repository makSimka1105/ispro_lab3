# 📐 GeometricLib

> Простая библиотека для расчёта геометрических параметров основных фигур

## 📁 Структура проекта

```
geometric-lib/
│
├── circle.py # Функции для круга
├── square.py # Функции для квадрата
├── rectangle.py # Функции для прямоугольника
├── funcs.py # Вспомогательные функции
└── docs
    └── README.md # Документация

```

---

## 🔧 Модули библиотеки

### 🔵 `circle.py` — Утилиты для круга

Работа с окружностями и кругами.

**Функции:**
- `area(r)` — вычисляет площадь круга
- `perimeter(r)` — вычисляет длину окружности

**Формулы:**
- Площадь: `S = π × r²`
- Периметр: `P = 2 × π × r`

---

### 🟦 `square.py` — Утилиты для квадрата

Расчёты для квадратов.

**Функции:**
- `area(a)` — вычисляет площадь квадрата
- `perimeter(a)` — вычисляет периметр квадрата

**Формулы:**
- Площадь: `S = a²`
- Периметр: `P = 4 × a`

---

### 🟩 `rectangle.py` — Утилиты для прямоугольника

Расчёты для прямоугольников.

**Функции:**
- `area(a, b)` — вычисляет площадь прямоугольника
- `perimeter(a, b)` — вычисляет периметр прямоугольника

**Формулы:**
- Площадь: `S = a × b`
- Периметр: `P = 2 × (a + b)`

---

### 🛠️ `funcs.py` — Вспомогательные функции

Дополнительные математические операции.

**Функции:**
- `square(n)` — возводит число в квадрат
- `add_binary(a, b)` — складывает два числа и возвращает результат в двоичной системе

---

## 📊 Сводная таблица

| **Фигура** | **Площадь** | **Периметр** |
|--------|---------|----------|
| **Круг** | `π × r²` | `2 × π × r` |
| **Квадрат** | `a²` | `4 × a` |
| **Прямоугольник** | `a × b` | `2 × (a + b)` |

---
## Примеры использования

```py3

import circle
import square 
import rectanglec
import funcs

print(circle.area(1)) # 3.14159...
print(circle.perimeter(1)) # 6.2831...

print(square.area(3)) # 9
print(square.perimeter(3)) # 12

print(rectangle.area(3,5)) # 15
print(rectangle.perimeter(3,5)) # 16

print(funcs.square(4))#16
print(funcs.square(10,100))#1010

```

## 📝 История изменений

### Версия 1.2 (2025-10-18)
- ✨ Добавлен модуль `funcs.py` с функциями `square()` и `add_binary()`
- 📚 Обновлена документация с примерами и визуализацией
- 🎨 Улучшено форматирование README

### Версия 1.1 (2025-10-17)
- ✨ Добавлен модуль `rectangle.py`
- 📝 Добавлены docstring во все функции

### Версия 1.0 (2025-10-16)
- 🎉 Первый комит
- ✨ Реализованы модули `circle.py` и `square.py`

---

## 👤 Автор

Проект создан в рамках лабораторной работы по Git и документации Markdown

## 📄 Лицензия

Свободное использование в учебных целях


